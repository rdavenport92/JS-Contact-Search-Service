{
  "name": "project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "pretest": "npm install --no-save",
    "test": "tsc && ./node_modules/.bin/mocha test --require @babel/register --colors",
    "test:watch": "concurrently \"tsc -w\" \"./node_modules/.bin/mocha test --require @babel/register --colors --watch\""
  },
  "dependencies": {
    "@babel/core": "7.14.3",
    "@babel/plugin-transform-runtime": "7.14.3",
    "@babel/preset-env": "7.14.2",
    "@babel/register": "7.13.16",
    "@babel/runtime": "7.14.0",
    "chai": "4.3.4",
    "mocha": "8.4.0",
    "mocha-junit-reporter": "2.0.0",
    "mocha-multi-reporters": "1.5.1"
  },
  "engines": {
    "node": "^14.0.0"
  },
  "devDependencies": {
    "concurrently": "^6.4.0",
    "typescript": "^4.5.3"
  }
}
